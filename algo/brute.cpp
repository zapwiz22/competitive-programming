#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve() {
    
}


signed main() {
    ios::sync_with_stdio(false), cin.tie(0);
    int TESTS = 1;
    cin >> TESTS;
    while (TESTS--) {
        solve();
    }
}
